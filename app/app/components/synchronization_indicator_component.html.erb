<div class="synchronizations-indicator" name="<%= name %>">
    <svg class="<%= svg_classes %>" aria-hidden="true" focusable="false" role="img">
        <use
            xlink:href="<%= icon_variant %>"></use>
    </svg>
    <span class="<%= label_classes %>"><%= content %></span>
</div>
