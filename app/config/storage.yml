local:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

amazon:
  service: S3
  bucket: <%= ENV["BUCKET_NAME"] %>

processed:
  service: S3
  bucket: <%= ENV["PROCESSED_BUCKET_NAME"] %>
