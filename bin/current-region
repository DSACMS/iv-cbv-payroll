#!/bin/bash
# Print the current AWS region
set -euo pipefail

result="$(aws configure get region)"

if [ -z "${result}" ] ; then
    result="${AWS_REGION}"
fi

if [ -z "${result}" ] ; then
    result="${AWS_DEFAULT_REGION}"
fi

echo -n "${result}"
